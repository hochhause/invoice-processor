# rules/default.py — default extraction patterns
# Copy this file and name it after your bank (e.g. ubs.py, postfinance.py).
# Set CSV_RULE_SET=ubs in .env to activate.
# Override only the keys you need; the rest inherit from extract.py defaults.

# PATTERNS = {
#     "invoice_id": [...],
#     "amount": [...],
#     ...
# }
