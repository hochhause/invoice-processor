# Desktop build deps — requirements.txt minus pyzbar (system zbar lib, not
# bundleable), pytest + xmlschema (DEV_MODE startup tests are excluded from
# the build), plus PyInstaller.
anthropic
fastapi==0.115.5
jinja2==3.1.4
numpy>=1.26.0
Pillow==11.1.0
pymupdf==1.24.14
python-multipart==0.0.12
uvicorn[standard]==0.32.1
zxing-cpp>=2.2.0
pyinstaller>=6.0
